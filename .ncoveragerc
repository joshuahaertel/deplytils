[run]
omit =
    # omit local files
    *local*
    # omit test files
    *test*
    # omit virtualenv files
    *.virtualenvs*
    # omit coverage files -- causes problems when using coverage
    *coverage*
